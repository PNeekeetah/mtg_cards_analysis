# Rating webapp

The webapp will display cards. Read the card effect, then rate it. You can use the rating guide as aid.

This project uses node. To start, make sure you run `node server.js`.

This app won't work without running the `python_analysis_app` first because it depends on the `reduced_card_index` to exist first.